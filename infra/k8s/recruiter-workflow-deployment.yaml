apiVersion: apps/v1
kind: Deployment
metadata:
  name: recruiter-workflow
  namespace: recruitment-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: recruiter-workflow
  template:
    metadata:
      labels:
        app: recruiter-workflow
    spec:
      containers:
        - name: recruiter-workflow
          image: recruiter-workflow:latest
          env:
            - name: SERVICE_NAME
              value: recruiter-workflow
            - name: PORT
              value: "8080"
            - name: CHAT_URL
              value: "http://chat:80"
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: recruiter-workflow
  namespace: recruitment-platform
spec:
  selector:
    app: recruiter-workflow
  ports:
    - port: 80
      targetPort: 8080
